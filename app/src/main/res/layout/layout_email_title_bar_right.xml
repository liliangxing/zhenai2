<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:paddingStart="@dimen/dimen_10dp"
    android:paddingEnd="@dimen/dimen_10dp">
    <FrameLayout
        android:id="@+id/layout_follow"
        android:background="@drawable/bg_no_follow"
        android:visibility="gone"
        android:layout_width="61dp"
        android:layout_height="25dp"
        android:layout_centerVertical="true">
        <TextView
            android:textSize="12sp"
            android:textColor="@color/white"
            android:gravity="center"
            android:layout_gravity="center"
            android:id="@+id/tv_follow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/follow"
            android:drawableStart="@drawable/icon_not_followed"/>
        <ImageView
            android:layout_gravity="center"
            android:id="@+id/iv_rotate"
            android:background="@drawable/icon_loading_follow"
            android:visibility="gone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </FrameLayout>
    <ImageView
        android:id="@+id/img_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/icon_black_menu"
        android:layout_centerVertical="true"
        android:layout_marginStart="5dp"
        android:layout_toEndOf="@+id/layout_follow"/>
</RelativeLayout>
