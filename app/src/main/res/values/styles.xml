<?xml version="1.0" encoding="utf-8"?>
<!--
  珍爱2 主题表(精简版)

  说明: 原 APK 反编译得到的 styles.xml 含 859 个 style,其中大量为 AppCompat /
        Material Components 库自带样式的副本(如 TextAppearance.AppCompat、
        Widget.AppCompat.* 等),与依赖库产生 duplicate resource 冲突。
        复刻代码的 UI 全部在 Kotlin 中编程式构建,不引用这些库样式;
        此处仅保留 AndroidManifest.xml 实际引用的应用主题及其父链。
-->
<resources>

    <!-- 应用基础主题: 对应原 AppTheme.Base,继承 AppCompat Light NoActionBar -->
    <style name="AppTheme.Base" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:includeFontPadding">false</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <!-- 主主题: Manifest 默认 android:theme -->
    <style name="AppTheme" parent="@style/AppTheme.Base">
        <item name="android:windowBackground">@color/background</item>
    </style>

    <style name="AppTheme.FullScreen" parent="@style/AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="@style/AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppThemeWithWhiteBackground" parent="@style/AppTheme">
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="AppThemeWithNoBackground" parent="@style/AppTheme">
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="AppThemeWithV8Background" parent="@style/AppTheme">
        <item name="android:windowBackground">@color/color_f2f4f5</item>
        <item name="colorControlActivated">@color/color_767dff</item>
    </style>

    <style name="AppThemeWithBlackBackground" parent="@style/AppTheme">
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- 半透明黑底带标题主题 -->
    <style name="ActivityTransparentBlack50WithTitleTheme" parent="@style/AppTheme">
        <item name="android:windowBackground">@color/black_50</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- 银联支付主题 -->
    <style name="UPPay" parent="">
        <item name="android:dialogTheme">@android:style/Theme.Dialog</item>
    </style>

    <!-- 全屏透明主题 -->
    <style name="fullScreenTranslucentStyle" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <!-- 对话框式 Activity 主题 -->
    <style name="ActivityDialogTheme" parent="@style/AppTheme.Base">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>

    <!-- 透明 Activity 主题(全屏) -->
    <style name="ActivityTransparentTheme" parent="@style/AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- 透明 Activity 主题(非全屏) -->
    <style name="ActivityTransparentThemeWithoutFullScreen" parent="@style/AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- 启动闪屏主题 -->
    <style name="AppTheme.ThemeSplash" parent="@style/AppTheme">
        <item name="android:windowBackground">@drawable/new_splash_bg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 半透明主题 -->
    <style name="AppTheme.Translucent" parent="@style/AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- 自动填充透明主题 -->
    <style name="AppThemeAutoFillTransparent" parent="@style/AppTheme.Base">
        <item name="android:windowBackground">@color/background</item>
    </style>

    <!-- 基础半透明(系统) -->
    <style name="Base_Translucent" parent="@android:style/Theme.Translucent">
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- 通用半透明 Activity -->
    <style name="CommonActivityTranslucent" parent="">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <!-- 核心半透明 Activity -->
    <style name="CoreActivityTranslucent" parent="">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowActionBar">false</item>
    </style>

    <!-- 无波纹主题 -->
    <style name="NoWaveTheme" parent="@style/AppTheme">
        <item name="android:windowBackground">@null</item>
        <item name="android:selectableItemBackground">@null</item>
    </style>

    <!-- 远程 WebView 主题 -->
    <style name="RemoteWebTheme" parent="@style/AppTheme">
        <item name="android:windowDisablePreview">true</item>
    </style>

    <!-- ShareSDK 主题 -->
    <style name="BaseSSSdkTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="colorAccent">@color/SSColorAccent</item>
        <item name="colorPrimary">@color/SSColorPrimary</item>
        <item name="colorPrimaryDark">@color/SSColorPrimaryDark</item>
    </style>
    <style name="SSSdkTheme" parent="@style/BaseSSSdkTheme">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 半透明主题(系统 NoTitleBar) -->
    <style name="TranslucentTheme" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>

    <!-- 微众银行人脸核身主题(精简:自定义 attr 已随反编译 attrs.xml 移除) -->
    <style name="wbcfFaceProtocolThemeBlack" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="wbcfFaceThemeBlack" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
